version: "3.9"
services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    env_file:
      - server/.env
    ports:
      - "4000:4000"

  client:
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - server
